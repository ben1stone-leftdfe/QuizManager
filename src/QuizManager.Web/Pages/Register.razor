<h3>Register</h3>

@if (RestrictedRegistered == false)
{
    <div class="btn btn-primary" @onclick="RegisterRestricted"> Register Editor </div>
}

@if (ViewOnlyRegistered == false)
{
    <div class="btn btn-primary" @onclick="RegisterViewOnly"> Register Editor </div>
}

@if (RestrictedRegistered == false)
{
    <div class="btn btn-primary" @onclick="RegisterEditor"> Register Editor </div>
}